export default class User{
    constructor(id,firstName,lastName,city){ // yapıcı blok, user newlendiğinde çalışır.
        this.id=id
        this.firstName=firstName
        this.lastName=lastName
        this.city=city
    }
}